# Benchmarks

This directory contains shared schemas and curated result locations. Experiment-specific raw evidence stays with the experiment. Use [`result.schema.json`](schemas/result.schema.json) for interoperable summaries and leave unavailable fields null or absent.
